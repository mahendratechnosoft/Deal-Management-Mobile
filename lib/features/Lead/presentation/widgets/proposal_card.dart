import 'package:flutter/material.dart';

class ProposalView extends StatelessWidget {
  const ProposalView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Proposal UI goes here',
        style: TextStyle(fontSize: 16),
      ),
    );
  }
}
